"""POST /api/ask — natural-language query. Filled in Step 7."""
from fastapi import APIRouter

router = APIRouter(prefix="/api", tags=["ask"])

# TODO Step 7: POST /api/ask -> AskResponse (rule-based parser; NIM/Nemotron stretch)
