# Optional. Leave UNSET in dev — the Vite proxy (vite.config.ts) forwards
# /api and /health to the backend on :8008, so the browser uses same-origin
# relative URLs and there is no CORS. Only set this for a non-proxied build.
# VITE_API_URL=http://localhost:8008
