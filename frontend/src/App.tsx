// Step 0 scaffold placeholder. Real composition (RiskMap3D + TimelineScrubber
// + ScenarioPanel + recommendation card) lands in Steps 4-5.
export default function App() {
  return (
    <div style={{ padding: 24 }}>
      <h1>Foresight for Fires</h1>
      <p>Backend forecast surface — scaffold. Wiring in Step 4.</p>
    </div>
  );
}
